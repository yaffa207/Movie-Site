@using פרויקט_MVC.Models; 
@model IEnumerable<Movy>
@{
    Layout = "~/Views/Layout.cshtml";
}

@*using System.Data.Linq;
	using System.Data.Linq.Mapping;
	using System.Data;
	using System.Collections.Generic;
	using System.Reflection;
	using System.Linq;
	using System.Linq.Expressions;
	using System.ComponentModel;
	using System;
    using System.ComponentModel.DataAnnotations;*@

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <style>
        table {
            border-collapse: collapse;
            border: 1px solid;
            margin-left: 380px;
            margin-top: 45px;
        }
        td, th {
            border: 1px solid black;
        }
        h1 {
            margin-left: 390px;
            font-size: 50px;
            color: wheat;
        }

        tr:nth-child(even) {
            border: 1px solid;
            width: 150px;
            height: 32px;
            text-align: center;
            font-size: 27px;
            color: wheat;
            background-color: rgba(0, 0, 0, 0.61);
        }

        tr:nth-child(odd) {

            border: 1px solid;
            width: 150px;
            height: 32px;
            text-align: center;
            font-size: 27px;
            color: black;
            background-color: rgba(245, 222, 179, 0.61);
        }


        li{
            list-style: none;
        }
        div {
            text-align: center;
            background-color: black;
            display: inline-block;
            margin-left: 275px;
            background-color: rgba(0, 0, 0, 0.00);
        }
        body {
            background-image: url('/Images/Cinema-Wallpaper-High-Definition.jpg');
            background-size: 100%;
        }
        .a {
            margin-left: 145px;
            background-color: black;
            background-color: rgba(0, 0, 0, 0.61);
        }
    </style>
</head>
<body>
    <br />
    <h1>All Movies</h1>
    <table>
        <tr>
   
            <th>Name</th>
            <th>Category</th>
        </tr>
        @foreach(Movy item in Model)
        {  
            <tr>
                <td>@item.Name</td>
                <td>@item.Category</td>
            </tr>
        }
    </table>

    <br /><br /><br />
    <div><a class="a" href="/Movies/New">Cilck to add movie</a></div>
</body>
</html>
